# Generated by Django 4.2.6 on 2023-10-24 21:20
import random

from django.db import (
    migrations,
    transaction,
)


# noinspection PyPep8Naming
def create_data(apps, _schema_editor):
    Author = apps.get_model("app2", "Author")
    Tag = apps.get_model("app2", "Tag")
    Article = apps.get_model("app2", "Article")

    with transaction.atomic():
        authors = [
            Author(name="Author One", bio="Bio One"),
            Author(name="Author Two", bio="Bio Two"),
            Author(name="Author Three", bio="Bio Three"),
        ]
        Author.objects.bulk_create(authors)

        articles = [
            Article(
                title=f"Article #{i}",
                content=f"Content for Article #{i}",
                author=authors[i % 3],
            )
            for i in range(1, 11)
        ]
        Article.objects.bulk_create(articles)

        tags = [Tag(name=f"Tag #{i}", description=f"Description for Tag #{i}") for i in range(1, 21)]
        Tag.objects.bulk_create(tags)

        for tag in Tag.objects.all():
            tag.articles.add(articles[random.randint(0, 9)])


class Migration(migrations.Migration):
    dependencies = [
        ("app2", "0001_initial"),
    ]

    operations = [migrations.RunPython(create_data, migrations.RunPython.noop)]
